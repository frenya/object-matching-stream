{
  "name": "object-matching-stream",
  "version": "1.0.0",
  "description": "A stream that matches incoming objects against a list of targets, finding the best match for every object.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive",
    "cover": "istanbul cover _mocha --report html -- --recursive -R spec -t 5000"
  },
  "keywords": [
    "object",
    "list",
    "match",
    "stream"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/frenya/object-matching-stream.git"
  },
  "author": "Frantisek Vymazal <frenya@f8.cz> (http://f8.cz)",
  "license": "MIT",
  "dependencies": {
    "debug": "^2.2.0",
    "fast-levenshtein": "^1.1.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "sinon": "^1.17.3"
  }
}
